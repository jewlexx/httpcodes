{
  "name": "@maybejules/httpcodes",
  "version": "0.1.0",
  "author": {
    "name": "Juliette Cordor",
    "email": "juliette@cordor.dev"
  },
  "description": "A simple library to get HTTP status codes",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jewlexx/httpcodes.git"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "exports": {
    ".": {
      "import": "./src/index.mjs",
      "require": "./dist/index.cjs",
      "types": "./dist/npm/index.d.ts"
    }
  },
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5 || ^7.0.0"
  },
  "scripts": {
    "build": "mkdir -p dist && bun run tsc && bun run build:cjs && bun run build:esm",
    "build:esm": "bun build index.ts --target browser --footer '// Made with love by Juliette Cordor' --outfile dist/index.mjs --minify",
    "build:cjs": "bun build index.ts --target browser --footer '// Made with love by Juliette Cordor' --outfile dist/index.cjs --minify --format cjs",
    "clean": "rm -rf dist"
  }
}
